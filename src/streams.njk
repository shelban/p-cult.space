---
layout: layouts/base.njk
title: стріми — культ особ
permalink: streams/index.html
---

{% include "chaos-decorations.njk" %}

<section class="streams-intro">
    <h2>стріми</h2>
    <p>Архів трансляцій, роздумів та єблоторгівлінгу</p>
</section>

<div class="streams-grid-container">
    {% for stream in collections.streams %}
    <article class="stream-card">
        {% if stream.thumbnail %}
        <a href="{{ stream.url }}" class="stream-card-thumbnail">
            <img src="{{ stream.thumbnail }}" alt="{{ stream.title }}" />
        </a>
        {% endif %}

        <div class="stream-card-content">
            <h3 class="stream-card-title">
                <a href="{{ stream.url }}">{{ stream.title }}</a>
            </h3>

            <p class="stream-card-meta">
                {{ stream.date | dateDisplay }}
                {% if stream.duration %} · {{ stream.duration }}{% endif %}
            </p>

            {% if stream.tags.length > 0 %}
            <div class="stream-card-tags">
                {% for tag in stream.tags %}<span class="stream-tag">{{ tag }}</span>{% endfor %}
            </div>
            {% endif %}

            <p class="stream-card-excerpt">{{ stream.source.templateContent | truncate(150) }}</p>
        </div>
    </article>
    {% endfor %}
</div>
